# AGENTS.md — 项目协作与代码规范

本文件约束在本项目中协作的 AI 代理和开发者。用户的明确要求优先于本文件；本文件未覆盖的内容遵循项目现有实现和主流社区实践。

## 1. 项目约定

- 项目使用 Next.js App Router、React、TypeScript 和 NestJS。
- Next.js 的路由由根目录 `app/` 下的文件系统约定负责。不要引入 React Router、重复维护自定义路由表，或把菜单配置当成路由注册器。
- 式神和御魂静态列表通过 HTTP 接口获取；其他适合服务端执行的业务操作使用 `next-safe-action` 封装的 Server Actions。
- 源码、文档和配置统一使用 UTF-8 无 BOM 编码；注释和面向用户的文字使用中文。
- 当前分支之外的文件和分支不在修改范围内；已有未提交修改属于用户，必须保留并避开无关重写。
- 修改文件前先读取目标路径上最近的 `AGENTS.md`、`README.md` 或贡献指南；越靠近目标文件的说明优先级越高。
- Next.js 的版本、配置和 API 以 `package.json`、存在时的 `node_modules/next/dist/docs/` 版本文档和官方文档为准，不凭旧版本记忆推断行为。

## 2. 工作流程

### 开始前

执行多文件修改、重构或排错时，先用中文说明任务理解和简短进度清单，再读取相关代码、配置、测试和日志。需求存在会改变实现方向的歧义时，先一次性提出最多 3 个问题，得到确认后再改动。

### 实施中

- 先修复明确问题，再考虑抽象和扩展；优先复用项目已有能力。
- 每次只改动当前目标范围，保留可回滚的小步变更。
- 不凭猜测确认结果；关键判断必须有代码、命令输出或运行现象依据。
- 删除核心文件、变更数据库结构、安装新依赖、修改生产配置，以及 `push`、`rebase`、`reset` 或强制覆盖等高风险操作，必须先征得用户确认。

### 交付时

说明完成内容、关键文件、验证命令和结果；同时说明未完成项、兼容性影响和部署注意事项。没有验证的内容不得表述为已完成。

## 3. 目录与文件组织

项目保持单仓库根目录结构，目录用于表达职责，文件名用于表达具体功能：

```text
/
├─ app/                    # Next.js App Router 的页面入口和框架特殊文件
├─ src/
│  ├─ components/          # 跨功能复用的 UI 组件
│  ├─ features/            # 按业务功能组织的页面和业务组件
│  ├─ config/              # 应用配置和静态展示配置
│  ├─ lib/                 # 服务调用、纯工具和基础适配层
│  ├─ store/               # Redux store、slice 和 selector
│  ├─ styles/              # 全局样式、设计令牌和公共样式
│  ├─ types/               # 跨文件复用的 TypeScript 类型
│  └─ ...
├─ server/                 # NestJS 服务端代码
├─ public/                 # 不需要构建处理的静态资源
└─ scripts/                # 开发、数据同步和部署辅助脚本
```

- 页面和可复用组件统一使用 PascalCase 功能文件夹；实现入口统一命名为 `index.tsx`，专属样式统一命名为 `index.module.scss`，例如 `PageNavigation/index.tsx`、`PageNavigation/index.module.scss`。修改文件名或移动文件时，必须同步修改所有 TSX/TS 的导入路径，并完成引用检查。
- 工具、算法、配置、格式化器和公共类型按功能使用有语义的文件名，例如 `relicApi.ts`、`calculator.ts`、`homeFormatters.ts`；不要为了套目录模板把这些普通模块拆成空文件夹或无意义的 `index.*`。
- 页面/组件目录必须包含实际接入的实现文件；禁止只创建空目录、只改文件名而不改调用方，或保留失效的旧入口。
- `app/` 中的 `page.tsx`、`layout.tsx`、`loading.tsx`、`error.tsx`、`not-found.tsx` 等是 Next.js 固定约定，可以保留固定文件名；它们应尽量只负责页面入口和编排，业务实现放在 `src/features/` 或 `src/components/`。
- `index.ts` 仅用于确实有必要的聚合导出，不作为每个组件、页面或类型的默认文件名。
- 一个实现文件只承担一个主要职责；发现文件同时承担页面、通用组件或基础服务职责时，按功能拆分，而不是继续堆叠。
- `.next/`、`dist/` 等构建产物和生成文件不可手工修改；需要改变它们时修改源代码或生成脚本。

## 4. React 与 TypeScript

### 组件与方法

- 一个 `.tsx` 文件原则上只定义一个主要 React 组件。
- 只被当前组件使用、需要访问组件状态或事件上下文的函数，直接写在组件内部；不要为了“看起来整齐”把它们机械地移到组件外。
- 纯函数、跨组件复用的逻辑、复杂计算或需要独立测试的逻辑，才提取到 `src/lib/` 或对应功能文件。
- 页面已经掌握数据且只负责直接渲染时，不要为了传递一组值额外创建只转发 Props 的中间层；只有跨边界复用、隔离状态或明确的组件职责需要时才定义 Props。
- 不要在组件函数体内声明另一个 React 组件；需要复用或独立维护的组件应提升到文件级并通过 Props 传值。
- 组件内部按“输入 → 状态 → 事件/派生逻辑 → JSX”组织，避免在 JSX 中堆叠长表达式。
- 组件必须覆盖加载、空数据、错误和成功等必要状态；异步操作要避免重复提交和竞态更新。
- 不要过度拆分页面、组件或算法：逻辑应保持在一个能读懂的功能文件中，只有接近两百行、职责明显分离、需要复用或需要独立测试时才拆分；拆分后不能产生只转发 Props、只导出常量或没有实际样式边界的空壳文件。

### 类型

- 类型很少且只服务当前文件时，直接在实现文件中声明，不额外创建类型文件。
- 多个文件、页面或服务共享的类型统一放在 `src/types/`，按领域或功能命名，例如 `src/types/relic.ts`、`src/types/account.ts`。
- 不在组件目录中创建 `*.types.ts`；组件 Props、事件和数据类型要么就地声明，要么从 `src/types/` 导入。
- 使用 `import type` 导入纯类型；避免重复声明同一数据结构，接口返回结构应与服务端保持一致。
- 运行时需要的 DTO、校验 schema 或 NestJS 装饰器类可以放在其服务端功能目录中，它们不等同于纯 TypeScript 类型。

### 导入与命名

- 优先使用项目已配置的 `@/*` 路径别名；近邻文件使用清晰的相对路径即可。
- 组件使用 PascalCase，普通模块和类型文件使用能表达功能的 camelCase；Next.js 固定约定文件名除外。
- 模块使用标准 ES Module 的 `import` / `export`，避免无必要的默认导出和循环依赖。

## 5. 样式规范

- 除 `src/styles/` 中的 reset、设计令牌、应用壳层、第三方组件覆盖和真正跨页面复用的公共规则外，所有页面和组件的私有样式必须使用 CSS Modules；禁止继续新增或依赖业务用的全局 `.scss` 选择器。
- 每个页面/组件的实现文件必须显式导入同目录的 `index.module.scss`，并实际使用导出的 `styles.xxx` 类名或明确的局部作用域类；不能只把 `.scss` 改名为 `.module.scss`，也不能保留无用的样式导入。
- CSS Modules 用于局部样式，避免组件间选择器冲突；页面级布局样式归页面，组件自身视觉样式归组件，不跨层级堆放。需要兼容第三方 DOM 类名时，只能在所属模块内使用范围明确的 `:global(...)`，并由该组件的局部类名作为作用域边界。
- TSX 中的样式引用必须与目录结构一致：同目录样式使用 `./index.module.scss`，移动组件后同步更新导入路径；禁止引用已删除的旧样式路径或依赖导入顺序“碰巧生效”。
- 全局样式只放 reset、设计令牌、根节点基础布局、全局滚动条、第三方组件覆盖和真正跨页面复用的动画；不得把页面或组件专属选择器继续塞进全局样式。
- 公共样式放入 `src/styles/`，具体样式使用有语义的文件名；不要新增无意义的 `styles.scss` 或业务用 `index.scss`。公共样式目录之外，页面/组件样式不得使用普通 `.scss` 文件。
- SCSS 使用模块化写法和 `@use`，合理复用变量、函数、mixin 和嵌套；不要把普通 CSS 原样改名为 `.scss`，也不要用 `!important` 或更高优先级掩盖错误的样式归属。
- 样式改动不得改变业务逻辑；涉及响应式布局时同时检查桌面端和移动端。

## 6. 数据访问与服务端代码

- 客户端组件不直接连接数据库；数据库访问集中在 NestJS 服务、Server Actions 或服务端数据访问模块中。
- 式神和御魂列表继续走公开 HTTP 接口，明确输入、输出、错误状态和空数据行为。
- Server Actions 统一通过 `next-safe-action` 定义输入校验、错误处理和返回结构；不要在页面中散落重复的 action 包装代码。
- 接口、Server Action 和数据库变更都要考虑参数校验、异常处理、幂等性和日志可读性；不得提交只能调用、不能正确处理失败情况的半成品。

## 7. 质量与验证

本项目协作中禁止自动执行生产构建、打包或其他会触发构建流程的命令。验证优先使用项目已有命令：

- TypeScript 类型检查：`yarn test`；如果 Yarn 无法解析本地二进制，执行 `node ./node_modules/typescript/bin/tsc --noEmit`。
- ESLint：`yarn lint`；Prettier：`node ./node_modules/prettier/bin/prettier.cjs --check <修改的文件>`。
- 与改动风险匹配的单元测试、接口测试或运行时冒烟验证。
- `git diff --check` 检查空白字符和补丁完整性。

验证失败时必须记录失败现象、复现命令、初步原因和后续处理，不得用跳过检查代替结论。

项目内不得生成、提交或依赖 `.log` 文件；运行日志输出到终端、系统临时目录或由启动工具管理。

### 完成前检查清单

1. 检查 `git status` 和 `git diff`，确认只包含本次目标修改。
2. 对修改范围运行类型检查、相关测试、ESLint 或 Prettier；不要只运行与改动无关的全量命令。
3. 运行 `git diff --check`，确认没有空白字符错误、临时文件或密钥。
4. 交付时明确记录未执行的构建、已知环境问题和需要用户手动完成的步骤。

## 8. Git 规则

- 只在当前用户指定的分支和工作区操作，不主动修改其他分支。
- 提交前检查 `git status`、`git diff` 和 `git diff --check`，确认没有把用户的无关修改混入提交。
- 提交标题要说明动作和范围；正文按需记录影响端、关键决策、兼容性注意事项和验证结果。
- `push` 必须得到用户明确确认；不得使用强制推送覆盖远程历史。
- 不维护内容重复且可能冲突的 `CLAUDE.md`、`GEMINI.md` 等代理规则文件；确有工具兼容需求时只保留指向根 `AGENTS.md` 的简短入口。
- 根 `AGENTS.md` 只记录所有任务都需要知道的事实；某个子系统规则明显变长时，在对应目录增加一份更近的 `AGENTS.md`，不要继续膨胀根文件。
